/** @type {import('next').NextConfig} */
// 更改为静态导出为dist文件夹，与其他项目打包同理
const nextConfig = {  
    //output: 'export',
    //distDir: "dist",
    //images: {
    //    unoptimized: true
    //}
};

module.exports = nextConfig
